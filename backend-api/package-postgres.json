{
	"name": "glim-backend-api",
	"version": "1.0.0",
	"description": "Backend API for Glim social platform",
	"main": "server.js",
	"scripts": {
		"start": "node server.js",
		"dev": "nodemon server.js",
		"migrate": "node -e \"require('fs').readFileSync('schema.sql', 'utf8')\""
	},
	"dependencies": {
		"bcrypt": "^6.0.0",
		"cors": "^2.8.5",
		"dotenv": "^17.2.2",
		"express": "^5.1.0",
		"jsonwebtoken": "^9.0.2",
		"pg": "^8.11.3"
	},
	"devDependencies": {
		"nodemon": "^3.1.10"
	},
	"engines": {
		"node": ">=18.0.0"
	}
}

